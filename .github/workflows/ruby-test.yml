name: Ruby SDK

on:
  workflow_dispatch:
  # push:
  #   paths:
  #     - 'planned/ruby/**'
  # pull_request:
  #   paths:
  #     - 'planned/ruby/**'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: planned/ruby
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true
        env:
          BUNDLE_GEMFILE: planned/ruby/Gemfile
      - run: bundle exec rspec --format documentation
