name: F# SDK

on:
  push:
    paths:
      - 'sdk/fsharp/**'
  pull_request:
    paths:
      - 'sdk/fsharp/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: |
          dotnet restore sdk/fsharp/Authdog.Sdk.FSharp.fsproj
          dotnet restore sdk/fsharp/tests/Authdog.Sdk.FSharp.Tests.fsproj
      - name: Build
        run: |
          dotnet build --no-restore -c Release sdk/fsharp/Authdog.Sdk.FSharp.fsproj
          dotnet build --no-restore -c Release sdk/fsharp/tests/Authdog.Sdk.FSharp.Tests.fsproj
      - name: Test
        run: dotnet test --no-build -c Release sdk/fsharp/tests/Authdog.Sdk.FSharp.Tests.fsproj --logger trx
